export interface IContent {
  name: string;
  thumbnail: string;
  mimeType: string;
  artist: string;
}

export interface IArtist {
  name: string;
  subscribers: string;
  thumbnail: string;
  mimeType: string;
}
